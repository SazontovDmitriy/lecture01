import math
b: str = input ("Длина первой стороны b=   ")
C: str = input ("Длина второй стороны c=  ")
d: str = input ("Введите величину угла d в радианах= ")
from math import pi
d: str =  (float(d))*(180/pi)
import math as m
a= math.sqrt((pow(float(C), 2)) + (pow(float(b), 2)) - (2 * (float(b)) * (float(C)) * (math.cos(float(d)))))
from math import cos
math.cos(float(d))
print("d=", math.cos(float(d)))
print("a=", a)

